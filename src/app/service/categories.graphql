query CategoryCbs(
  $locale: I18NLocaleCode
  $categorySlug: String
  $categoryId: ID
) {
  categoryCbs(
    locale: $locale
    filters: { Slug: { eq: $categorySlug }, id: { eq: $categoryId } }
  ) {
    ...CategoryCbEntityResponseCollectionFragment
  }
}